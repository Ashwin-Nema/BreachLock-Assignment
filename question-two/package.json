{
  "name": "parking-lot",
  "version": "1.0.0",
  "description": "A simple server that manages a parking lot that is rate-limited by the IP Address",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ashwin Nema",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^6.0.10"
  }
}
